import 'package:flutter/material.dart';

class SelectContactToCall extends StatelessWidget {
  const SelectContactToCall({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Select Contact to call screen"),
      ),
    );
  }
}
